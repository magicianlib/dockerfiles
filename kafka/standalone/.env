# Kafka 对外宣告地址（EXTERNAL 监听器宣告给客户端的地址），按场景启用其一：
# 1. 本机开发、客户端都跑在宿主机上：用 localhost
# 2. 容器内客户端要接入宿主机上的 Kafka（如 kafka-ui 容器，Mac/Docker Desktop 环境实测 IP 不可达时）：用 host.docker.internal
# 3. 远程部署、局域网其他机器的客户端接入：用宿主机实际 IP
ADVERTISED_HOST=localhost
#ADVERTISED_HOST=host.docker.internal
#ADVERTISED_HOST=机器IP
