# kafka-ui 加入的 Kafka compose 网络名，按实际运行的 Kafka 目录启用其一：
# 1. 运行 debezium-connect 这套（当前默认）
KAFKA_NETWORK=debezium-connect_default
# 2. 运行 standalone：改用下行
#KAFKA_NETWORK=standalone_default
# 3. 运行 cluster：改用下行（界面里接入地址填 kafka1:19092）
#KAFKA_NETWORK=cluster_default
